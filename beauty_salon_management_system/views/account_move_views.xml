<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Inherit account.move form view to add appointment field -->
        <record id="view_move_form_inherit_beauty_salon" model="ir.ui.view">
            <field name="name">account.move.form.inherit.beauty.salon</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='invoice_date']" position="after">
                    <field name="appointment_id" widget="many2one" options="{'no_create': True}"/>
                    <field name="appointment_date" widget="datetime" options="{'datepicker': true}"/>
                </xpath>
            </field>
        </record>
        <!-- Inherit account.move tree view to add appointment date -->
        <record id="view_move_tree_inherit_beauty_salon" model="ir.ui.view">
            <field name="name">account.move.tree.inherit.beauty.salon</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='invoice_date']" position="after">
                    <field name="appointment_date"/>
                </xpath>
            </field>
        </record>
        <!-- Inherit account.move search view to add appointment date -->
        <record id="view_move_search_inherit_beauty_salon" model="ir.ui.view">
            <field name="name">account.move.search.inherit.beauty.salon</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_search"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='invoice_date']" position="after">
                    <field name="appointment_date"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>